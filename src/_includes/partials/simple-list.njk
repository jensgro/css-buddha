{% if loop.first %}<ul>{% endif %}
    <li>
        <a href="{{ post.url }}">{{ post.data.title }}</a> 
        {% if post.data.date %}
        <span>{{post.data.date | htmlDateString}}</span> 
        {% endif %}
    </li>
{% if loop.last %}</ul>{% endif %}
