{% if loop.first %}<ul class="simple-list">{% endif %}
    <li>
        <a href="{{ post.url }}">{{ post.data.title }}</a> 
        {% if post.data.date %}
        <span class="post-date">{{post.data.date | htmlDateString}}</span> 
        {% endif %}
    </li>
{% if loop.last %}</ul>{% endif %}
